# RPC Lab – Distributed Computing

## Description
This project implements a minimal Remote Procedure Call (RPC) system using Python sockets and JSON serialization. The client and server run on separate AWS EC2 instances.

## How to Run

### Server (EC2 Instance B)
```bash
python3 server.py
```

### Client (EC2 Instance A)
```bash
python3 client.py
